import {textSpan} from '../../../common/cElement';

export var current = textSpan('Current');
export var kills = textSpan('Kills');
export var member = textSpan('Member');
export var pctTotal = textSpan('% of Total');
export var status = textSpan('Status');
export var titanHp = textSpan('Titan HP');
export var total = textSpan('Total');
export var yourGuild = textSpan('Your Guild');
